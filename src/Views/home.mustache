{{ >partials/header }}

<p>Cards List</p>

<ul>
{{ #cards }}
  <li>
    <span>{{ type.description }}<span>
    <p>{{ title }}</p>
    <img src="{{ image }}" width="150" height="150" />
  </li>
{{ /cards }}
</ul>

{{ >partials/footer }}
